# CLAUDE.md — surfn-papirus-deeporange

## Project overview

Papirus **deeporange** folder colour, folders-only, inheriting `surfn-icons`. Generated by
`~/.bin/make-papirus-colour surfn deeporange` from the installed Papirus theme.

## Current state

Ships `usr/share/icons/Surfn-Papirus-Deeporange/` (places only). Packaged as `surfn-papirus-deeporange-icons-git`, `depends=('surfn-icons-git')`.

## Patterns & decisions

- Folders only (generic folder*.svg recoloured to deeporange); rest inherits surfn-icons.
  Symlinks dereferenced (0 broken). `icon-theme.cache` gitignored. Regenerate with make-papirus-colour.
